echo "<pre>";
var_dump($carrito);
echo"</pre>";


//Acceder al valor de un arreglo con la superglobal $_SERVER
  echo "<pre>";
  var_dump($_SERVER["REQUEST_METHOD"]);
  echo"</pre>";

//foreach para ejecutar errores de un arreglo si los hay
  <?php foreach($errores as $error): ?>
    <?php echo $error; ?>  
  <?php endforeach; ?>
