<?php include 'includes/header.php';

// Boolean: datos que solo pueden ser true o false
$logueado = true;
var_dump($logueado);

// Enteros
$numero = 200;
var_dump($numero);

// Floats: números no enteros
$float = 200.5;
var_dump($float);

// Strings: cadenas de texto
$nombre = "Zeky";
var_dump($nombre);

$array = [];
var_dump($array);


include 'includes/footer.php';